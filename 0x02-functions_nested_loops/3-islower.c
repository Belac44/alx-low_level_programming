#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 * islower - check if upper or lowercase
 *
 * Return: 1 if true and o otherwise
 */
int _islower(int c)
{
    if (islower(c))
        return (1);
    return (0);
}